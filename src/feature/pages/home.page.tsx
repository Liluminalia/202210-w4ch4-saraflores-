import { RobotsList } from '../components/robots.list/robots.list';

function HomePage() {
    return (
        <main>
            <h2>Robots</h2>
            <RobotsList></RobotsList>
        </main>
    );
}

export default HomePage;
